{
  "name": "gantryworksmock",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler deploy --assets=./dist"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}